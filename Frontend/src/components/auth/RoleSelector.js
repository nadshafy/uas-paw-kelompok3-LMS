import React from "react";

const RoleSelector = ({ value, onChange }) => {
  // UI disembunyikan (return null) karena pendaftaran otomatis menjadi Member.
  // Pendaftaran Librarian dilakukan lewat database/admin khusus.
  return null;
};

export default RoleSelector;
